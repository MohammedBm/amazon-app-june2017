<!DOCTYPE html>
<html>
  <head>
    <title>Amaonz2017</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>

    <!-- <div>
      <span>
        <%= link_to 'Home',home_path %>
      </span>
      |
      <span>
        <%= link_to 'About',about_path %>
      </span>
      |
      <span>
        <%= link_to 'Contact',contact_path %>
      </span>
      |
      <span>
        <%= link_to 'New Product', new_product_path %>
      </span>
      |
      <span>
        <%= link_to 'All Products', products_path	 %>
      </span>
      |
      <% if user_singed_in? && can?(:manage, @product) %>
      <%= link_to 'Dashboard', admin_dashboard_index_path	 %>
      <% end %>
      |
      <% if user_singed_in?%>
      <span>Hello,
        <%= current_user.full_name %>!</span>
      <span><%= link_to 'Log Out', sessions_path, method: :delete, data:{confirm:'Log Out?'} %></span>
    <% else %>
      <span><%= link_to 'Sign In', new_session_path%>
        |</span>
      <span><%= link_to 'Create an Account', new_user_path%>
        |</span>
      <% end %>

      <hr class ='hr'/>
    </div> -->

    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Amazon App</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

          <!-- left side -->
          <ul class="nav navbar-nav">
            <li><%= link_to 'Home',home_path %></li>
            <li><%= link_to 'About',about_path %></li>
            <li><%= link_to 'New Product',new_product_path %></li>
            <li><%= link_to 'All Product',products_path %></li>
            <li><%= link_to 'About',about_path %></li>
            <li>
              <% if user_singed_in? && can?(:manage, @product) %>
              <%= link_to 'Dashboard', admin_dashboard_index_path	 %>
              <% end %>
            </li>

          </ul>
          <!-- right side  -->
          <ul class="nav navbar-nav navbar-right">
            <% if user_singed_in?%>
            <li class = 'navbarHello'>Hello,
              <%= current_user.full_name %>!
            </li>
            <li>
              <%= link_to 'Log Out', sessions_path, method: :delete, data:{confirm:'Log Out?'} %>
            </li>
          <% else %>
            <li>
              <%= link_to 'Sign In', new_session_path%>
            </li>
            <li>
              <%= link_to 'Create an Account', new_user_path%>
            </li>
            <% end %>
          </ul>
        </div>
        <!-- /.navbar-collapse -->
      </div>
      <!-- /.container-fluid -->
    </nav>

    <% if flash[:notice] %>
    <div class='alert alert-success'>
      <%= flash[:notice] %>
    </div>
    <% elsif flash[:alert] %>
    <div class='alert alert-danger'>
      <%= flash[:alert] %>
    </div>
    <% end %>

    <div class="container">
      <%= yield %>
    </div>

  </body>
</html>
